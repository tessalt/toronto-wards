{
  "name": "toronto-wards",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "parcel watch index.html",
    "cp:data": "cp data/wards.json dist/data/wards.json",
    "clean": "rm -rf dist/data && mkdir dist/data",
    "start": "npm run clean && npm run cp:data && npm run watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "csv-parser": "^1.12.1",
    "date-fns": "^1.29.0",
    "geojson-geometries-lookup": "^0.2.5",
    "leaflet": "^1.3.1",
    "leaflet-ajax": "^2.1.0",
    "leaflet.offline": "^1.0.3",
    "localforage": "^1.7.2",
    "papaparse": "^4.5.0"
  },
  "devDependencies": {
    "cssnano": "^4.0.3",
    "parcel-plugin-json-url-loader": "^0.1.1"
  }
}
